<p id="notice"><%= notice %></p>

<h1>Listing Jiras</h1>

<table>
  <thead>
    <tr>
      <th>Issuekey</th>
      <th>Projectname</th>
      <th>Component</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>   

  <tbody>
   
<% b = Time.now() %>
    <% @jiras.each do |jira| %>
      <tr>
        <td><%= jira.issuekey %></td>
        <td><%= jira.projectname %></td>
        <td><%= link_to jira.status, jiras_componentview_url(jira.status) %></td>
   <!--
   <td><%= jira.hoursintriage %></td>
       <td><%= Time.now() %></td>
     
<td><%= (Time.now()-jira.hoursintriage)/3600 %></td> 
        <td><%= link_to 'Show', jira %></td>
        <td><%= link_to 'Edit', edit_jira_path(jira) %></td>
        <td><%= link_to 'Destroy', jira, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    -->
    <% end %>
  </tbody>
</table>

<br>


<%= link_to  'Weekly data', jiras_weeklyview_path %>
<%= link_to  'High level view', jiras_highlevel_path %>

